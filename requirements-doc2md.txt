# doc2md — document extraction. No LLM, no network, no API key.
pymupdf>=1.24
python-docx>=1.1
python-pptx>=0.6.23
EbookLib>=0.18
lxml>=5.0
beautifulsoup4>=4.12
markdownify>=0.13
Pillow>=10.0
PyYAML>=6.0
# Extras cover the formats routed to the MarkItDown fallback (.xlsx/.xls/.msg).
markitdown[xlsx,xls,outlook]>=0.1
